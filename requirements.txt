# Dev dependencies. The Toga backend is platform-specific:
#   - Windows: toga-winforms
#   - macOS:   toga-cocoa
#   - Linux:   toga-gtk
# Briefcase handles the platform split via pyproject.toml; this file is just
# for direct `pip install -r requirements.txt` development on Windows.
boto3>=1.34.0
toga>=0.5.0
toga-winforms>=0.5.0; sys_platform == "win32"
toga-cocoa>=0.5.0; sys_platform == "darwin"
toga-gtk>=0.5.0; sys_platform == "linux"
